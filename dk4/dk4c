#!/usr/bin/env bash
set -euo pipefail

if [ $# -lt 1 ]; then
    echo "usage: $(basename "$0") <source.dk4>" >&2
    exit 1
fi

input="$1"
base="${input%.*}"

"$(dirname "$0")/dk4" "$input" > "${base}.c"
gcc -O2 "${base}.c" -o "${base}" -lm
